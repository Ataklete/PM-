
spring:
  application:
    name: shoppingcart
  datasource:
    url: jdbc:mysql://localhost/pm
    username: pm
    password: pm123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
server:
  port: 8001